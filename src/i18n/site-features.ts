/**
 * Launch gates — routes stay in the repo but remain hidden from nav / sitemap / index until enabled.
 * @see docs/site-architecture.md (utility / deferred routes)
 */
export const blogPublic = false;
